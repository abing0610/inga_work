<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inga.entity.Province">

  <resultMap type="Province" id="pro">
  	<id column="province" property="province"/>
    <result column="name" property="name"/>
    
  </resultMap>

  <select id="getAllProvince" parameterType="map" resultMap="pro">
    select * from inga_provin
  </select>
 <!--  
  <update id="updateUserByCondition" parameterType="User">
    update user
    <set>
      <if test="uname != null">
        username = #{uname} ,
      </if>
      <if test="address != null">
        address = #{address}
      </if>
    </set>
    where id = #{id}
  </update> -->

</mapper>